<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:id="@+id/home_page"
    android:orientation="vertical" >

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:gravity="bottom"
        android:orientation="horizontal"
        android:background="@drawable/toolbar_bk">

        <ImageView
            android:background="@drawable/default_avatar"
            android:layout_marginLeft="22dp"
            android:layout_marginBottom="14dp"
            android:layout_width="@dimen/main_page_avatar"
            android:layout_height="@dimen/main_page_avatar"
            android:id="@+id/avatar"/>

    </LinearLayout>

    <fragment
        android:id="@+id/travel_tip"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        class="com.zhizulx.tt.ui.fragment.TravelTipFragment"
        tools:layout="@layout/tt_fragment_travel_tip" />
</LinearLayout>